{
  "name": "core",
  "version": "1.0.0",
  "description": "Meme core API.",
  "main": "src/server.ts",
  "scripts": {
    "format": "prettier-eslint \"src/**/*.ts\"",
    "fix": "prettier-eslint --write \"src/**/*.ts\"",
    "start": "yarn build && node dist/server.js",
    "dev": "ts-node-dev --ignore-watch node_modules src/server.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Danilo Peixoto and Willer Silva",
  "license": "BSD-3-Clause",
  "dependencies": {
    "axios": "^0.21.0",
    "dotenv": "^8.2.0",
    "mongoose": "^5.11.1",
    "restify": "^8.5.1",
    "restify-router": "^0.6.2"
  },
  "devDependencies": {
    "@types/restify": "^8.4.2",
    "prettier-eslint-cli": "^5.0.0",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.5",
    "yarn": "^1.22.10"
  }
}
